---
const { 
  experienceYears = [],
  experience = [],
  trustedBy = [],
  serviceOfferings = [],
  processSteps = [],
  contact = {}
} = Astro.props;

// Sort experience by years descending and calculate fill percentages
const sortedExperience = [...experienceYears].sort((a, b) => b.years - a.years);
const maxYears = sortedExperience[0]?.years || 1;
const experienceWithFill = sortedExperience.map(exp => ({
  ...exp,
  fillPercent: (exp.years / maxYears) * 100
}));
---

<section class="audit-request-page">
  <!-- Hero Header Section -->
  <div class="audit-header">
    <div class="header-content">
      <div class="header-left">
        <div class="badge-container">
          <span class="status-badge">Available for Projects</span>
        </div>
        <h1 class="main-title">Security Audits</h1>
        <p class="subtitle">Comprehensive security assessments for Web3 organizations</p>
        
        <div class="quick-stats">
          <div class="stat-item">
            <div class="stat-number">10+</div>
            <div class="stat-label">Years Experience</div>
          </div>
          <div class="stat-item">
            <div class="stat-number">50+</div>
            <div class="stat-label">Audits Completed</div>
          </div>
          <div class="stat-item">
            <div class="stat-number">24/7</div>
            <div class="stat-label">Incident Response</div>
          </div>
        </div>
      </div>
      
      <div class="header-right">
        <div class="contact-card">
          <h3>Get Started</h3>
          <p>30-minute consultation</p>
          <a href="https://calendly.com/d/cnqr-49f-xdy/auditware-consultation" class="cta-button-primary">
            Schedule Consultation
          </a>
          <div class="contact-divider"></div>
          <div class="contact-info">
            <div class="info-item">
              <svg class="icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
              </svg>
              <span>contact@auditware.io</span>
            </div>
            <div class="info-item">
              <svg class="icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <span>Response within 24 hours</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="content-wrapper">
    <!-- Left Column: Skills & Experience -->
    <aside class="sidebar">
      <!-- Experience -->
      <div class="section-card experience-card">
        <div class="section-header">
          <div class="section-icon">
            <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
            </svg>
          </div>
          <h2>Experience</h2>
        </div>
        <div class="experience-years-grid">
          {experienceWithFill.map((exp, index) => (
            <div class="experience-year-item">
              <div class="experience-top">
                <span class="experience-year-label">{exp.name}</span>
              </div>
              <div class="experience-bar">
                <div class="experience-fill" style={`width: ${exp.fillPercent}%`}></div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <div class="slim-section">
        <div class="slim-header">
          <h4>Comply with industry standards</h4>
        </div>
        <div class="compliance-badges">
          <a href="https://github.com/W3OSC/web3-opsec-standard" target="_blank" rel="noopener noreferrer" title="W3OS Compliance">
            <img 
              src="/images/W3OS-Graphic.png" 
              alt="W3OS Compliance" 
              class="compliance-icon" 
            />
          </a>
          <a href="https://frameworks.securityalliance.org/certs/overview" target="_blank" rel="noopener noreferrer" title="SEAL Certification">
            <img 
              src="https://images.typeform.com/images/85jp8NBEtu4u" 
              alt="SEAL Certification" 
              class="compliance-icon" 
            />
          </a>
        </div>
      </div>

      <!-- Audit Experience - Compact -->
      <div class="section-card compact-card">
        <div class="section-header-simple">
          <h3>Audit Background</h3>
        </div>
        <ul class="experience-list">
          {experience.map(item => (
            <li class="experience-item">
              <div class="experience-dot"></div>
              <span>{item}</span>
            </li>
          ))}
        </ul>
      </div>

      <!-- Trusted By & Compliance - Slim combined -->
      <div class="slim-section">
        <div class="slim-header">
          <h4>Trusted By</h4>
        </div>
        <div class="clients-grid-compact">
          {trustedBy.map(client => (
            <img 
              src={client.logo} 
              alt={client.name} 
              width={client.width || 360} 
              height={client.height || 120}
              class="client-logo-compact" 
              loading="lazy" 
            />
          ))}
        </div>
      </div>
    </aside>

    <!-- Right Column: Service Offerings -->
    <main class="main-content">
      <!-- Service Packages -->
      <div class="section-card main-card">
        <div class="section-header">
          <div class="section-icon">
            <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
            </svg>
          </div>
          <h2>Service Offerings</h2>
        </div>
        
        <div class="services-grid" id="services-grid">
          {serviceOfferings.map((service, index) => (
            <div class={`service-card ${service.featured ? 'featured' : ''} ${service.featured ? 'expanded' : ''}`} data-expanded={service.featured}>
              {service.featured && <div class="featured-badge">Most Popular</div>}
              <div class="service-header">
                <h3 class="service-title">{service.title}</h3>
                {service.tagline && <p class="service-tagline">{service.tagline}</p>}
              </div>
              
              <div class="service-description">
                <p>{service.description}</p>
              </div>

              <div class="details-indicator">
                <span class="indicator-text">View Details</span>
                <svg class="indicator-arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </div>

              <div class="service-details">
                <div class="details-content">
                  <div class="service-features">
                    <div class="features-header">What's Included:</div>
                    <ul class="features-list">
                      {service.features.slice(0, 4).map(feature => (
                        <li class="feature-item">
                          <svg class="check-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                          </svg>
                          <span>{feature}</span>
                        </li>
                      ))}
                    </ul>
                  </div>

                  {service.deliverables && (
                    <div class="service-deliverables">
                      <div class="deliverables-header">Key Deliverables:</div>
                      <ul class="deliverables-list">
                        {service.deliverables.slice(0, 3).map(deliverable => (
                          <li class="deliverable-item">
                            <svg class="doc-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                            </svg>
                            <span>{deliverable}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                  )}
                </div>
              </div>

              {service.duration && (
                <div class="service-meta">
                  <div class="meta-item">
                    <svg class="meta-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <span>{service.duration}</span>
                  </div>
                </div>
              )}
            </div>
          ))}
        </div>
      </div>

    </main>
  </div>
</section>

<style>
  .audit-request-page {
    background: transparent;
    min-height: 100vh;
    padding-bottom: 80px;
    position: relative;
    overflow: hidden;
  }

  /* Dotted Background Pattern - Removed, using SiteBackground instead */

  /* Header Section - More Compact */
  .audit-header {
    background: transparent;
    backdrop-filter: blur(3px);
    -webkit-backdrop-filter: blur(3px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    padding: 60px 20px 40px;
    margin-bottom: 40px;
    position: relative;
    z-index: 1;
    animation: fade-in-up 0.6s var(--ease-out);
  }

  .header-content {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: 60px;
    align-items: start;
    animation: fade-in 0.8s 0.2s var(--ease-out) backwards;
  }

  .header-left {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .badge-container {
    display: flex;
    gap: 12px;
  }

  .status-badge {
    display: inline-flex;
    align-items: center;
    padding: 8px 16px;
    background: rgba(34, 197, 94, 0.1);
    border: 1px solid rgba(34, 197, 94, 0.3);
    border-radius: 24px;
    color: #4ade80;
    font-size: 0.875rem;
    font-weight: 500;
    position: relative;
  }

  .status-badge::before {
    content: '';
    width: 8px;
    height: 8px;
    background: #4ade80;
    border-radius: 50%;
    margin-right: 8px;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .main-title {
    font-size: 3.5rem;
    font-weight: 800;
    color: #ffffff;
    line-height: 1.1;
    margin: 0;
    letter-spacing: -0.02em;
  }

  .subtitle {
    font-size: 1.25rem;
    color: #a0a0a0;
    line-height: 1.5;
    max-width: 600px;
  }

  .quick-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-top: 12px;
  }

  .stat-item {
    text-align: left;
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1;
    margin-bottom: 8px;
  }

  .stat-label {
    font-size: 0.875rem;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 500;
  }

  .header-right {
    position: sticky;
    top: 100px;
  }

  .contact-card {
    background: linear-gradient(135deg, rgba(125, 211, 252, 0.04) 0%, rgba(0, 0, 0, 0.4) 100%);
    border: 1px solid rgba(30, 58, 138, 0.3);
    border-radius: 24px;
    padding: 32px;
    backdrop-filter: blur(12px);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  }

  .contact-card h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 8px;
  }

  .contact-card > p {
    color: #a0a0a0;
    margin-bottom: 24px;
    font-size: 1rem;
  }

  .contact-divider {
    height: 1px;
    background: rgba(255, 255, 255, 0.1);
    margin: 24px 0;
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .info-item {
    display: flex;
    align-items: center;
    gap: 12px;
    color: #a0a0a0;
    font-size: 0.875rem;
  }

  .info-item .icon {
    width: 20px;
    height: 20px;
    color: #1e3a8a;
    flex-shrink: 0;
  }

  /* Content Layout */
  .content-wrapper {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
    display: grid;
    grid-template-columns: 380px 1fr;
    gap: 40px;
    align-items: start;
    animation: slide-in-up 0.8s 0.3s var(--ease-out) backwards;
  }

  @keyframes slide-in-up {
    from {
      transform: translateY(30px);
    }
    to {
      transform: translateY(0);
    }
  }

  .sidebar {
    position: sticky;
    top: 100px;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .main-content {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  /* Section Cards */
  .section-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    padding: 20px;
    backdrop-filter: blur(8px);
    transition: all 0.3s ease;
  }

  .section-card:hover {
    border-color: rgba(30, 58, 138, 0.3);
    box-shadow: 0 4px 24px rgba(30, 58, 138, 0.1);
  }

  .main-card {
    background: rgba(255, 255, 255, 0.03);
    padding: 32px;
  }

  .compact-card {
    padding: 16px;
  }

  /* Slim Sections (for Trusted By and Compliance) */
  .slim-section {
    background: rgba(255, 255, 255, 0.015);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    padding: 12px 16px;
    backdrop-filter: blur(6px);
  }

  .slim-header h4 {
    font-size: 0.8125rem;
    font-weight: 600;
    color: #999;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 10px;
  }

  .section-header-simple {
    margin-bottom: 14px;
    padding-bottom: 10px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  .section-header-simple h3 {
    font-size: 0.875rem;
    font-weight: 600;
    color: #ffffff;
    margin: 0;
  }

  .section-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 24px;
    padding-bottom: 16px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .section-icon {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(30, 58, 138, 0.1);
    border-radius: 10px;
    color: #1e3a8a;
    flex-shrink: 0;
  }

  .section-icon svg {
    width: 20px;
    height: 20px;
  }

  .section-header h2 {
    font-size: 1.25rem;
    font-weight: 600;
    color: #ffffff;
    margin: 0;
  }

  /* Experience Years */
  .experience-years-grid {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .experience-year-item {
    display: flex;
    flex-direction: column;
    gap: 6px;
    transition: all 0.3s ease;
    padding: 4px;
    border-radius: 8px;
  }

  .experience-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .experience-year-label {
    font-size: 0.8125rem;
    color: #cccccc;
    font-weight: 500;
    transition: color 0.3s ease;
  }

  .experience-bar {
    height: 6px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 3px;
    overflow: hidden;
    position: relative;
  }

  .experience-fill {
    height: 100%;
    background: linear-gradient(90deg, #1e3a8a 0%, #1e40af 100%);
    border-radius: 3px;
    transition: all 0.8s ease;
    box-shadow: 0 0 8px rgba(30, 58, 138, 0.3);
  }

  /* Experience List */
  .experience-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .experience-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    color: #a0a0a0;
    font-size: 0.9375rem;
    line-height: 1.6;
  }

  .experience-dot {
    width: 8px;
    height: 8px;
    background: #1e3a8a;
    border-radius: 50%;
    margin-top: 7px;
    flex-shrink: 0;
  }

  /* Clients Grid - Compact */
  .clients-grid-compact {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    align-items: center;
    justify-items: center;
  }

  .client-logo-compact {
    max-width: 100%;
    height: 80px;
    width: auto;
    opacity: 0.6;
    transition: opacity 0.3s ease;
    filter: grayscale(100%) brightness(1.2);
    object-fit: contain;
  }

  .client-logo-compact:hover {
    opacity: 1;
    filter: grayscale(0%) brightness(1);
  }

  /* Compliance Badges */
  .compliance-badges {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
  }

  .compliance-badges a {
    display: inline-flex;
    transition: all 0.3s ease;
    opacity: 0.8;
  }

  .compliance-badges a:hover {
    opacity: 1;
    transform: translateY(-2px);
  }

  .compliance-icon {
    height: 40px;
    width: auto;
    object-fit: contain;
  }

  /* Service Cards */
  .services-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
  }

  .service-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    padding: 24px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    cursor: pointer;
  }

  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #1e3a8a 0%, #1e40af 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .service-card:hover::before {
    opacity: 1;
  }

  .service-card.featured {
    border-color: rgba(30, 58, 138, 0.3);
    background: rgba(30, 58, 138, 0.05);
  }

  .service-card.featured::before {
    opacity: 1;
  }

  .featured-badge {
    position: absolute;
    top: 16px;
    right: 16px;
    background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
    color: #ffffff;
    padding: 4px 10px;
    border-radius: 16px;
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .service-card:hover {
    border-color: rgba(30, 58, 138, 0.4);
    box-shadow: 0 8px 32px rgba(30, 58, 138, 0.15);
    transform: translateY(-2px);
  }

  /* Service Details Toggle */
  .service-details {
    margin-top: 0;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease, margin-top 0.3s ease;
  }

  .service-card.expanded .service-details,
  .service-card[data-expanded="true"] .service-details {
    max-height: 2000px;
    margin-top: 16px;
  }

  .details-content {
    animation: slideDown 0.3s ease;
  }

  /* Details Indicator */
  .details-indicator {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-top: 12px;
    color: #1e3a8a;
    font-size: 0.875rem;
    font-weight: 500;
    transition: all 0.3s ease;
    opacity: 1;
  }

  .service-card.expanded .details-indicator,
  .service-card[data-expanded="true"] .details-indicator {
    opacity: 0;
    max-height: 0;
    margin-top: 0;
    overflow: hidden;
  }

  .indicator-text {
    transition: color 0.3s ease;
  }

  .indicator-arrow {
    width: 16px;
    height: 16px;
    transition: transform 0.3s ease;
    color: #1e3a8a;
  }

  .service-card:hover .details-indicator {
    color: #1e40af;
  }

  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .service-header {
    margin-bottom: 12px;
  }

  .service-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 6px;
  }

  .service-tagline {
    font-size: 0.875rem;
    color: #1e3a8a;
    font-weight: 500;
  }

  .service-description {
    margin-bottom: 16px;
  }

  .service-description p {
    color: #a0a0a0;
    line-height: 1.6;
    font-size: 0.9375rem;
  }

  .features-header,
  .deliverables-header {
    font-size: 0.75rem;
    font-weight: 600;
    color: #999;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 10px;
  }

  .features-list,
  .deliverables-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .feature-item,
  .deliverable-item {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    color: #cccccc;
    font-size: 0.875rem;
    line-height: 1.5;
  }

  .check-icon,
  .doc-icon {
    width: 16px;
    height: 16px;
    color: #4ade80;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .doc-icon {
    color: #1e3a8a;
  }

  .service-features {
    margin-bottom: 16px;
  }

  .service-deliverables {
    margin-bottom: 16px;
  }

  .service-meta {
    display: flex;
    gap: 16px;
    padding-top: 12px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    margin-top: 12px;
  }

  .meta-item {
    display: flex;
    align-items: center;
    gap: 6px;
    color: #a0a0a0;
    font-size: 0.8125rem;
  }

  .meta-icon {
    width: 16px;
    height: 16px;
    color: #1e3a8a;
  }

  /* Process Timeline - Compact Horizontal */
  .process-timeline-compact {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 8px;
    padding: 12px;
    background: rgba(255, 255, 255, 0.01);
    border-radius: 12px;
  }

  .process-step-compact {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }

  .step-card {
    width: 100%;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    padding: 12px;
    transition: all 0.3s ease;
  }

  .step-card:hover {
    background: rgba(30, 58, 138, 0.05);
    border-color: rgba(30, 58, 138, 0.3);
    transform: translateY(-2px);
  }

  .step-top {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 8px;
  }

  .step-number-compact {
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
    border-radius: 50%;
    font-size: 0.875rem;
    font-weight: 700;
    color: #ffffff;
    flex-shrink: 0;
    box-shadow: 0 2px 8px rgba(30, 58, 138, 0.3);
  }

  .step-title-compact {
    font-size: 0.875rem;
    font-weight: 600;
    color: #ffffff;
    margin: 0;
    flex: 1;
    line-height: 1.3;
  }

  .step-duration-compact {
    display: inline-block;
    padding: 2px 8px;
    background: rgba(30, 58, 138, 0.1);
    border: 1px solid rgba(30, 58, 138, 0.2);
    border-radius: 10px;
    color: #1e3a8a;
    font-size: 0.6875rem;
    font-weight: 500;
    white-space: nowrap;
  }

  .step-details {
    width: 100%;
  }

  .step-summary {
    cursor: pointer;
    color: #1e3a8a;
    font-size: 0.75rem;
    font-weight: 500;
    list-style: none;
    user-select: none;
    padding: 4px 0;
    transition: color 0.3s ease;
  }

  .step-summary::-webkit-details-marker {
    display: none;
  }

  .step-summary::after {
    content: ' ▼';
    font-size: 0.625rem;
  }

  .step-details[open] .step-summary::after {
    content: ' ▲';
  }

  .step-summary:hover {
    color: #1e40af;
  }

  .step-description-compact {
    color: #999;
    font-size: 0.8125rem;
    line-height: 1.5;
    margin-top: 8px;
    animation: slideDown 0.3s ease;
  }

  .step-arrow {
    position: absolute;
    right: -12px;
    top: 50%;
    transform: translateY(-50%);
    color: rgba(30, 58, 138, 0.3);
    font-size: 1.25rem;
    z-index: 1;
  }

  /* CTA Buttons */
  .cta-button-primary,
  .cta-button-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 14px 28px;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    cursor: pointer;
    border: none;
  }

  .cta-button-primary {
    background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
    color: #ffffff;
    box-shadow: 0 4px 16px rgba(30, 58, 138, 0.4);
  }

  .cta-button-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(30, 64, 175, 0.5);
  }

  .cta-button-secondary {
    background: rgba(255, 255, 255, 0.05);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .cta-button-secondary:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(30, 58, 138, 0.3);
    transform: translateY(-2px);
  }

  .cta-button-primary.large,
  .cta-button-secondary.large {
    padding: 18px 36px;
    font-size: 1.125rem;
  }

  /* Final CTA */
  .final-cta {
    margin-top: 20px;
    background: linear-gradient(135deg, rgba(125, 211, 252, 0.04) 0%, rgba(186, 230, 253, 0.025) 100%);
    border: 1px solid rgba(30, 58, 138, 0.3);
    border-radius: 24px;
    padding: 48px;
    text-align: center;
  }

  .cta-content h3 {
    font-size: 2rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 12px;
  }

  .cta-content p {
    font-size: 1.125rem;
    color: #a0a0a0;
    margin-bottom: 32px;
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Responsive Design */
  @media (max-width: 1199px) {
    .header-content {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .header-right {
      position: static;
    }

    .content-wrapper {
      grid-template-columns: 1fr;
      gap: 32px;
    }

    .sidebar {
      display: none;
    }

    .process-timeline-compact {
      grid-template-columns: repeat(2, 1fr);
    }

    .step-arrow {
      display: none;
    }
  }

  @media (max-width: 767px) {
    .audit-header {
      padding: 40px 20px 30px;
    }

    .main-title {
      font-size: 2rem;
    }

    .subtitle {
      font-size: 1rem;
    }

    .quick-stats {
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
    }

    .stat-number {
      font-size: 1.5rem;
    }

    .sidebar {
      grid-template-columns: 1fr;
    }

    .section-card,
    .main-card {
      padding: 20px;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: stretch;
    }

    .final-cta {
      padding: 32px 24px;
    }

    .cta-content h3 {
      font-size: 1.5rem;
    }

    .cta-content p {
      font-size: 1rem;
    }

    .process-timeline-compact {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  // Make service cards clickable to toggle details
  document.addEventListener('DOMContentLoaded', () => {
    const serviceCards = document.querySelectorAll('.service-card');
    
    serviceCards.forEach(card => {
      card.addEventListener('click', (e) => {
        // Don't toggle if clicking on a link inside the card
        const target = e.target as HTMLElement;
        if (target && target.closest && target.closest('a')) {
          return;
        }
        
        const isExpanded = card.classList.contains('expanded');
        const expandedAttr = card.getAttribute('data-expanded') === 'true';
        
        if (isExpanded || expandedAttr) {
          card.classList.remove('expanded');
          card.setAttribute('data-expanded', 'false');
        } else {
          card.classList.add('expanded');
          card.setAttribute('data-expanded', 'true');
        }
      });
    });
  });
</script>

