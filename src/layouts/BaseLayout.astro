---
import '../styles/tokens.css';
import '../styles/global.css';
import SiteBackground from '../components/SiteBackground.astro';

export interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonical?: string;
  structuredData?: any;
  googleAnalyticsId?: string;
  frontmatter?: {
    title?: string;
    description?: string;
    image?: string;
    canonical?: string;
    googleAnalyticsId?: string;
  };
}

// Handle both direct props and MDX frontmatter
const props = Astro.props;
const frontmatter = props.frontmatter || {};
const title = props.title || frontmatter.title || "Auditware";
const description = props.description || frontmatter.description || "Operational security audits for web3 organizations, active compromise monitoring, and rapid incident response.";
const image = props.image || frontmatter.image || "/images/HP-Grph-image.png";
const canonical = props.canonical || frontmatter.canonical;
const structuredData = props.structuredData || frontmatter.structuredData;
const googleAnalyticsId = props.googleAnalyticsId || frontmatter.googleAnalyticsId;

// Construct canonical URL - use provided canonical or generate from current URL
const siteUrl = 'https://auditware.io';
const canonicalUrl = canonical || new URL(Astro.url.pathname, siteUrl).toString();
const imageUrl = image.startsWith('http') ? image : new URL(image, siteUrl).toString();

// Default Organization Schema
const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "Auditware",
  "url": siteUrl,
  "logo": new URL("/images/HP-Grph-image.png", siteUrl).toString(),
  "description": "Premier Web3 operational security audits, smart contract audits, and the Sentry OpSec monitoring platform.",
  "foundingDate": "2020",
  "sameAs": [
    "https://twitter.com/auditware",
    "https://linkedin.com/company/auditware"
  ],
  "contactPoint": {
    "@type": "ContactPoint",
    "contactType": "Sales",
    "url": `${siteUrl}/audits`
  }
};

// Website Schema for site search
const websiteSchema = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "Auditware",
  "url": siteUrl,
  "description": description,
  "publisher": {
    "@type": "Organization",
    "name": "Auditware"
  }
};

// Combine schemas
const allSchemas = [organizationSchema, websiteSchema];
if (structuredData) {
  allSchemas.push(structuredData);
}
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="theme-color" content="#000000" />
  <title>{title}</title>
  <meta name="description" content={description} />
  
  <!-- Canonical URL -->
  <link rel="canonical" href={canonicalUrl} />
  
  <!-- Open Graph -->
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={imageUrl} />
  <meta property="og:url" content={canonicalUrl} />
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="Auditware" />
  <meta property="og:locale" content="en_US" />
  
  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:site" content="@auditware" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={imageUrl} />
  
  <!-- Structured Data (JSON-LD) -->
  <script type="application/ld+json" set:html={JSON.stringify(allSchemas)} />
  
  <!-- Favicon -->
  <link href="/images/favicon.png" rel="shortcut icon" type="image/x-icon" />
  <link href="/images/webclip.png" rel="apple-touch-icon" />
  
  <!-- DNS Prefetch & Preconnect for Performance -->
  <link rel="dns-prefetch" href="https://fonts.googleapis.com" />
  <link rel="dns-prefetch" href="https://fonts.gstatic.com" />
  <link rel="dns-prefetch" href="https://www.google-analytics.com" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  
  <!-- Fonts - Optimized Loading with display=swap -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet" />
  
  <!-- Google Analytics -->
  {googleAnalyticsId && (
    <>
      <script async src={`https://www.googletagmanager.com/gtag/js?id=${googleAnalyticsId}`}></script>
      <script define:vars={{ googleAnalyticsId }}>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', googleAnalyticsId);
      </script>
    </>
  )}
</head>
<body>
  <SiteBackground />
  <slot />
  
  <script>
    // Scroll reveal animations
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('active');
        }
      });
    }, observerOptions);

    // Observe all elements with the reveal class
    document.addEventListener('DOMContentLoaded', () => {
      const revealElements = document.querySelectorAll('.reveal');
      revealElements.forEach(el => observer.observe(el));
      
      // Add scroll shadow to navbar
      const navbar = document.querySelector('.fixed-navbar');
      if (navbar) {
        window.addEventListener('scroll', () => {
          if (window.scrollY > 20) {
            navbar.classList.add('scrolled');
          } else {
            navbar.classList.remove('scrolled');
          }
        });
      }
    });
  </script>
</body>
</html>

<style is:global>
  /* Global Styles */
  * {
    box-sizing: border-box;
  }

  body {
    color: #fff;
    background: transparent;
    font-family: var(--font-family-body);
    font-size: 14px;
    line-height: 1.2;
    margin: 0;
    padding: 0;
    position: relative;
  }
  
  body > * {
    position: relative;
    z-index: 1;
  }

  h1 {
    margin-top: 20px;
    margin-bottom: 0;
    font-size: var(--font-size-10xl);
    font-weight: 500;
    line-height: 0.9;
    color: #fff;
  }

  h2 {
    margin-top: 20px;
    margin-bottom: 10px;
    font-size: var(--font-size-7xl);
    font-weight: 500;
    line-height: 1.2;
    color: #fff;
  }

  h3 {
    margin-top: 20px;
    margin-bottom: 10px;
    font-size: var(--font-size-6xl);
    font-weight: 500;
    line-height: 1.2;
    color: #fff;
  }

  h4 {
    margin-top: 10px;
    margin-bottom: 10px;
    font-size: var(--font-size-4xl);
    font-weight: 500;
    line-height: 1.2;
    color: #fff;
  }

  h5 {
    margin-top: 10px;
    margin-bottom: 10px;
    font-size: var(--font-size-5xl);
    font-weight: 500;
    line-height: 1.2;
    color: #fff;
  }

  h6 {
    margin-top: 10px;
    margin-bottom: 10px;
    font-size: var(--font-size-3xl);
    font-weight: 500;
    line-height: 1.2;
    color: #fff;
  }

  p {
    color: #fff;
    margin-bottom: 10px;
    font-family: var(--font-family-body);
    font-size: var(--font-size-base);
    font-weight: 400;
    line-height: 1.3;
  }

  a {
    color: #fff;
    text-decoration: none;
  }

  img {
    max-width: 100%;
    display: inline-block;
  }

  /* Container utility */
  .container {
    width: 100%;
    max-width: var(--max-width-container);
    margin: 0 auto;
    padding: 0 2em;
  }

  /* Responsive typography */
  @media screen and (max-width: 991px) {
    h1 { font-size: var(--font-size-7xl); }
    h2 { font-size: var(--font-size-6xl); }
    h3 { font-size: var(--font-size-5xl); }
    h4 { font-size: var(--font-size-4xl); }
    h5 { font-size: var(--font-size-3xl); }
    h6 { font-size: var(--font-size-2xl); }
  }

  @media screen and (max-width: 767px) {
    h1 { font-size: var(--font-size-5xl); }
    h2 { font-size: var(--font-size-4xl); }
    h3 { font-size: var(--font-size-3xl); }
    h4 { font-size: var(--font-size-2xl); }
    h5 { font-size: var(--font-size-2xl); }
    h6 { font-size: var(--font-size-xl); }
  }
</style>
